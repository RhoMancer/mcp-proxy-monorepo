---
phase: 02-local-access
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp-http-proxy/examples/local-only.config.js
  - packages/mcp-http-proxy/README.md
autonomous: false
requirements:
  - CONFIG-03

must_haves:
  truths:
    - "Example config file demonstrates local-only mode without OAuth"
    - "Config comments explain local-only usage clearly"
    - "README documents differences between local and tunnel configurations"
    - "User can run proxy locally by following README instructions"
  artifacts:
    - path: "packages/mcp-http-proxy/examples/local-only.config.js"
      provides: "Local-only configuration template"
      contains: "export default"
    - path: "packages/mcp-http-proxy/README.md"
      provides: "Local mode documentation"
      contains: "Local Development"
  key_links:
    - from: "README.md Local Development section"
      to: "examples/local-only.config.js"
      via: "code block example"
      pattern: "examples/local-only.config.js"
    - from: "Config comments"
      to: "Local mode explanation"
      via: "JSDoc comments"
      pattern: "NO auth|local only|OAuth disabled"
---

<objective>
Create comprehensive local-only configuration example and documentation for Claude Code local development

Purpose: CONFIG-03 requires an example config demonstrating local-only mode without OAuth. While hevy-local.config.js and libreoffice-local.config.js exist, they are specific to particular MCP servers. A generic local-only.config.js template with clear documentation will help users understand local mode configuration.

Output: Generic local-only.config.js template and updated README with local mode documentation
</objective>

<execution_context>
@C:/Users/harry/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/harry/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-local-access/02-RESEARCH.md
@packages/mcp-http-proxy/examples/hevy-local.config.js
@packages/mcp-http-proxy/examples/libreoffice-local.config.js
@packages/mcp-http-proxy/README.md

# Existing Local Config Examples

From hevy-local.config.js:
- Uses npx -y hevy-mcp command
- Omits auth and oauthProvider keys (local mode)
- Includes inline comments explaining NO OAuth

From libreoffice-local.config.js:
- Uses Python with -m libreoffice_calc_mcp
- Omits auth and oauthProvider keys
- Includes requirements in comments

# Current README Structure

README.md has sections for:
- OAuth 2.0 Redirect Mode
- OAuth Provider Mode (for Claude.ai Connectors)
- Configuration Examples
- Running the Proxy

Missing: Dedicated local mode section explaining how to use without OAuth
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generic local-only.config.js template</name>
  <files>packages/mcp-http-proxy/examples/local-only.config.js</files>
  <action>Create a generic, well-documented local-only configuration template:

1. Start with comprehensive JSDoc comment block:
```javascript
/**
 * Local-Only Configuration for MCP HTTP Proxy
 *
 * This configuration runs the proxy in LOCAL mode without any OAuth authentication.
 * Use this for local Claude Code CLI development where you don't need external access.
 *
 * LOCAL MODE CHARACTERISTICS:
 * - No OAuth authentication required
 * - Accessible from localhost only
 * - Direct connection to MCP server via stdio
 * - CORS enabled for all origins
 *
 * USAGE:
 *   cd packages/mcp-http-proxy
 *   node src/cli.js -c ../examples/local-only.config.js
 *
 * CLAUDE CODE CONFIGURATION:
 *   Add to your Claude Code .claude/config.json:
 *   {
 *     "mcpServers": {
 *       "my-local-server": {
 *         "url": "http://localhost:8080/sse"
 *       }
 *     }
 *   }
 *
 * DIFFERENCES FROM OAUTH MODES:
 * - Local mode: No auth/oauthProvider keys, accessible from localhost
 * - OAuth redirect: Requires 'auth' config, redirects to GitHub/Google login
 * - OAuth Provider: Requires 'oauthProvider' config, for Claude.ai Connectors
 */
```

2. Use echo-mcp-server as default (works without external dependencies):
```javascript
export default {
  mcp: {
    // The MCP server command to run
    command: 'node',
    args: [
      // Path to echo-mcp-server relative to monorepo root
      // Replace with your actual MCP server command
      '../examples/test-server/echo-mcp-server.js'
    ],
    // Optional: Working directory for the MCP process
    cwd: undefined, // Set to path if MCP server needs specific working directory
    // Optional: Environment variables to pass to MCP server
    env: {
      // Example: API_KEY: process.env.YOUR_API_KEY
    }
  },
  server: {
    // HTTP port for the proxy (choose a port not in use)
    port: 8080,
    // Bind to localhost only for security (use 0.0.0.0 for network access)
    host: '127.0.0.1'
  }
  // NOTE: NO 'auth' or 'oauthProvider' keys = local mode (no authentication)
};
```

3. Include inline comments explaining each configuration option
4. Add troubleshooting section in comments at the bottom
5. Reference the differences between local mode and OAuth modes

File should be usable as-is with echo-mcp-server for testing.</action>
  <verify>
    <automated>cd packages/mcp-http-proxy && node -e "import('../examples/local-only.config.js').then(c => console.log('Config loaded:', Object.keys(c.default)))"</automated>
  </verify>
  <done>local-only.config.js loads successfully, has no auth/oauthProvider keys, includes comprehensive comments</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify local-only.config.js documentation quality</name>
  <what-built>Generic local-only.config.js template with comprehensive documentation comments</what-built>
  <how-to-verify>
    1. Read the generated config file: packages/mcp-http-proxy/examples/local-only.config.js
    2. Verify comments clearly explain local mode characteristics
    3. Verify config is usable as-is (or with minimal edits)
    4. Verify differences from OAuth modes are documented
    5. Check that JSDoc comment includes Claude Code configuration example
  </how-to-verify>
  <resume-signal>Type "approved" or describe improvements needed</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update README.md with local mode documentation</name>
  <files>packages/mcp-http-proxy/README.md</files>
  <action>Add a dedicated "Local Development Mode" section to README.md:

1. Add new section after "Features" and before "OAuth 2.0 Redirect Mode":
```markdown
## Local Development Mode

Use local mode when developing with Claude Code CLI on your machine. No authentication required.

### Configuration

Create a config file that omits the `auth` and `oauthProvider` keys:

\`\`\`javascript
// local.config.js
export default {
  mcp: {
    command: 'npx',
    args: ['-y', 'your-mcp-server']
  },
  server: {
    port: 8080,
    host: '127.0.0.1'
  }
  // NO auth or oauthProvider keys = local mode
};
\`\`\`

### Usage

1. Start the proxy:
\`\`\`bash
node src/cli.js -c path/to/local.config.js
\`\`\`

2. Configure Claude Code (.claude/config.json):
\`\`\`json
{
  "mcpServers": {
    "my-server": {
      "url": "http://localhost:8080/sse"
    }
  }
}
\`\`\`

### Local vs. OAuth Modes

| Mode | Config Keys | Use Case | Authentication |
|------|-------------|----------|----------------|
| **Local** | (none) | Claude Code CLI development | None |
| **OAuth 2.0** | `auth` | Personal use with OAuth providers | GitHub/Google/etc |
| **OAuth Provider** | `oauthProvider` | Claude.ai Connectors (tunnel) | PKCE/Client Credentials |

See [examples/local-only.config.js](examples/local-only.config.js) for a complete template.
```

2. Update the "Configuration Examples" section to include local mode:
```markdown
### Local Mode (No Auth)

- [local-only.config.js](examples/local-only.config.js) - Generic template for local Claude Code development
- [hevy-local.config.js](examples/hevy-local.config.js) - Hevy workout tracker MCP server
- [libreoffice-local.config.js](examples/libreoffice-local.config.js) - LibreOffice Calc MCP server
```

3. Keep existing OAuth Provider and OAuth 2.0 sections unchanged

Place local mode section prominently (before OAuth sections) since it's simpler and recommended for local development.</action>
  <verify>
    <automated>grep -A 5 "Local Development Mode" packages/mcp-http-proxy/README.md</automated>
  </verify>
  <done>README.md contains Local Development Mode section with config examples, usage instructions, and mode comparison table</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify README.md documentation quality</name>
  <what-built>Updated README.md with local mode documentation section</what-built>
  <how-to-verify>
    1. Read packages/mcp-http-proxy/README.md
    2. Find "Local Development Mode" section
    3. Verify configuration example is clear and accurate
    4. Verify Claude Code setup instructions are complete
    5. Verify comparison table shows differences between modes
    6. Verify local-only.config.js is referenced in the documentation
  </how-to-verify>
  <resume-signal>Type "approved" or describe improvements needed</resume-signal>
</task>

</tasks>

<verification>
Manual verification steps (after approval):

1. Test the config template works:
```bash
cd packages/mcp-http-proxy
node src/cli.js -c ../examples/local-only.config.js
```
Verify proxy starts without errors and shows "Auth: Disabled" in startup banner.

2. Test Claude Code can connect:
- Add to .claude/config.json: `{"mcpServers": {"echo": {"url": "http://localhost:8080/sse"}}}`
- Restart Claude Code
- Verify echo server tools are available

3. Verify documentation is accurate:
- README.md local mode section is accurate
- Config file comments match actual behavior
- All referenced files exist
</verification>

<success_criteria>
1. local-only.config.js exists and is valid JavaScript
2. Config has no auth/oauthProvider keys (local mode)
3. Config includes comprehensive comments explaining local mode
4. README.md has Local Development Mode section
5. README explains differences between local and OAuth modes
6. Both checkpoints approved by user
7. Example config works when tested
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-access/02-03-SUMMARY.md`
</output>
